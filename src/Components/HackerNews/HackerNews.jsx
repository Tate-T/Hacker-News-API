


function HeckerNews({users}) {
// console.dir(users)
    return (
        <>
        {/* {users.map(user => {
            console.log(user)
            return(
                <>
                <p>{user.title}</p> 
                </>
            );
        })} */}
        </>
    );
};

export default HeckerNews;